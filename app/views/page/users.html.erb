<div class="album-cards">
  <% @users.each do |user| %>
    <div class="card">
      <h3><%= user.name %></h3>
      <img src="<%= Faker::Avatar.image%>"/>
      <%= link_to "voir", "/user/#{user.id}" %>
    </div>
  <% end %>
</div>
