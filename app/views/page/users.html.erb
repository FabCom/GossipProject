<h1>UTILISATEURS</h1>
<div class="album-cards">
  <% @users.each do |user| %>
    <div class="card">
      <h3><%= user.name %></h3>
      <img src="<%= Faker::Avatar.image%>"/>
      <p class="card-link"><%= link_to "voir", "/user/#{user.id}" %></p>
    </div>
  <% end %>
</div>
